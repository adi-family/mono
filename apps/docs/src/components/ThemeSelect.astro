{/* ADI docs are dark-mode only (design/DESIGN.md §3) — the picker is removed rather than left
   pointing at a theme the site doesn't support. Paired with the ThemeProvider override, which
   fixes the theme instead of reading it from this. */}

# Build output and probe artefacts. `dist/` is what gets deployed and is rebuilt by
# `scripts/build-mesh-client.sh`; `harness/pkg/` is wasm-pack's, for the probe page only.
/dist
# wrangler's own scratch state, written next to whatever directory it is invoked from.
/.wrangler
/harness/pkg
/harness/report.json
/harness/report-*.json
# The URL the harness last opened — it carries a live pairing token. So does what `mesh invite`
# printed, and so — readably, as a QR — does the video the fake camera plays back.
/harness/*-url.txt
/harness/invite.txt
/harness/camera.y4m
/harness/browser.pem
/harness/.chrome-profile*
/harness/.chrome-e2e*
/harness/*.log

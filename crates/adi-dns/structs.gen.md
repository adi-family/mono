<!-- @generated by `cargo run -p adi-structs` — do not edit by hand. -->

# `adi-dns` — types

> adi-family local DNS resolver: split-DNS overrides + upstream forwarding, run in the foreground under a supervisor

4 structs across 3 files.

## Index

- [`src/config.rs`](#srcconfigrs) — `Config`, `OverrideZone`
- [`src/main.rs`](#srcmainrs) — `AdiHandler`
- [`src/status.rs`](#srcstatusrs) — `Status`

---

## `src/config.rs`

### struct `Config`

```rust
#[derive(Debug, Clone, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct Config {
    #[serde(default = "default_bind_addr")]
    pub bind_addr: IpAddr,
    #[serde(default = "default_preferred_port")]
    pub preferred_port: u16,
    #[serde(default = "default_fallback_ports")]
    pub fallback_ports: Vec<u16>,
    #[serde(default = "default_domain")]
    pub domain: String,
    #[serde(default = "default_upstreams")]
    pub upstreams: Vec<SocketAddr>,
    #[serde(default)]
    pub manage_os_routing: bool,
    #[serde(default)]
    pub overrides: Vec<OverrideZone>,
    #[serde(default)]
    pub status_file: Option<PathBuf>,
}
```

### struct `OverrideZone`

```rust
#[derive(Debug, Clone, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct OverrideZone {
    pub suffix: String,
    pub address: IpAddr,
}
```

---

## `src/main.rs`

### struct `AdiHandler`

Split-DNS overrides first, then upstream forwarding.

```rust
struct AdiHandler {
    overrides: Vec<(LowerName, IpAddr)>,
    resolver: TokioResolver,
}
```

---

## `src/status.rs`

### struct `Status`

```rust
#[derive(Debug, Serialize)]
pub struct Status {
    pub pid: u32,
    pub domain: String,
    pub bound_addr: String,
    pub port: u16,
    pub route_installed: bool,
    pub started_at_unix: u64,
    pub version: String,
}
```


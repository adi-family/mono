<!-- @generated by `cargo run -p adi-structs` — do not edit by hand. -->

# `adi-indexer` — types

> The code indexer: tree-sitter parsing into a per-project SQLite index of files, symbols, and the call graph, searchable by name (FTS5), by path, or by meaning (jina code embeddings over a usearch vector index). Pure library — `adi-mono indexer` is its CLI.

72 structs · 10 enums · 2 type aliases across 38 files.

## Index

- [`src/analyzer/entry_points.rs`](#srcanalyzerentry_pointsrs) — `EntryPointDetector`
- [`src/analyzer/filters.rs`](#srcanalyzerfiltersrs) — `AnalysisMode`, `AnalysisConfig`, `DeadCodeFilter`
- [`src/analyzer/mod.rs`](#srcanalyzermodrs) — `DeadCodeAnalyzer`
- [`src/analyzer/reachability.rs`](#srcanalyzerreachabilityrs) — `ReachabilityAnalyzer`
- [`src/analyzer/report.rs`](#srcanalyzerreportrs) — `ReportFormat`, `DeadCodeReport`, `ReportSummary`
- [`src/cache.rs`](#srccachers) — `CachedFileData`, `GlobalCache`
- [`src/clones.rs`](#srcclonesrs) — `CloneGroup`
- [`src/config.rs`](#srcconfigrs) — `Config`, `ParserConfig`, `StorageConfig`, `IndexConfig`, `IgnoreConfig`
- [`src/embed/candle.rs`](#srcembedcandlers) — `BertEmbeddings`, `BertSelfAttention`, `BertSelfOutput`, `BertAttention`, `BertGLUMLP`, `BertLayer`, `JinaBertModel`, `CandleEmbedder`
- [`src/embed/config.rs`](#srcembedconfigrs) — `EmbeddingConfig`, `ConfigWrapper`
- [`src/embed/error.rs`](#srcembederrorrs) — `EmbedError`, `Result`
- [`src/embed/mod.rs`](#srcembedmodrs) — `NoEmbedder`
- [`src/error.rs`](#srcerrorrs) — `Error`, `Result`
- [`src/graph.rs`](#srcgraphrs) — `SymbolMetrics`
- [`src/indexer/mod.rs`](#srcindexermodrs) — `FileProcessResult`
- [`src/lang/cpp.rs`](#srclangcpprs) — `InternalSymbolKind`, `InternalReferenceKind`, `InternalLocation`, `InternalSymbol`, `InternalReference`, `CppAnalyzer`
- [`src/lang/csharp.rs`](#srclangcsharprs) — `CSharpAnalyzer`
- [`src/lang/go.rs`](#srclanggors) — `GoAnalyzer`
- [`src/lang/java.rs`](#srclangjavars) — `JavaAnalyzer`
- [`src/lang/lua.rs`](#srclangluars) — `LuaAnalyzer`
- [`src/lang/php.rs`](#srclangphprs) — `PhpAnalyzer`
- [`src/lang/python.rs`](#srclangpythonrs) — `PythonAnalyzer`
- [`src/lang/ruby.rs`](#srclangrubyrs) — `RubyAnalyzer`
- [`src/lang/rust.rs`](#srclangrustrs) — `RustAnalyzer`
- [`src/lang/swift.rs`](#srclangswiftrs) — `SwiftAnalyzer`
- [`src/lang/typescript.rs`](#srclangtypescriptrs) — `TypeScriptAnalyzer`
- [`src/lib.rs`](#srclibrs) — `Indexer`
- [`src/migrations/runner.rs`](#srcmigrationsrunnerrs) — `SqlMigration`
- [`src/parser/treesitter/analyzers/base.rs`](#srcparsertreesitteranalyzersbasers) — `AnalyzerBase`
- [`src/parser/treesitter/analyzers/generic.rs`](#srcparsertreesitteranalyzersgenericrs) — `GenericAnalyzer`
- [`src/parser/treesitter/mod.rs`](#srcparsertreesittermodrs) — `TreeSitterParser`
- [`src/search/usearch.rs`](#srcsearchusearchrs) — `UsearchIndex`
- [`src/storage/mmap.rs`](#srcstoragemmaprs) — `Header`, `EmbeddingStore`, `EmbeddingIterator`
- [`src/storage/mod.rs`](#srcstoragemodrs) — `StructureRow`
- [`src/storage/sqlite.rs`](#srcstoragesqliters) — `SqliteStorage`
- [`src/structure.rs`](#srcstructurers) — `Structure`
- [`src/types.rs`](#srctypesrs) — `SymbolId`, `FileId`, `Visibility`, `SymbolKind`, `Location`, `Symbol`, `File`, `FileInfo`, `SearchResult`, `Tree`, `FileNode`, `SymbolNode`, `Status`, `IndexProgress`, `Language`, `ReferenceKind`, `ParsedReference`, `Reference`, `SymbolUsage`, `ParsedSymbol`, `ParsedFile`
- [`src/watcher.rs`](#srcwatcherrs) — `Watcher`

---

## `src/analyzer/entry_points.rs`

### struct `EntryPointDetector`

```rust
#[derive(Debug)]
pub struct EntryPointDetector {
    storage: Arc<dyn Storage>,
}
```

---

## `src/analyzer/filters.rs`

### enum `AnalysisMode`

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub enum AnalysisMode {
    Strict,
    Library,
    Application,
}
```

### struct `AnalysisConfig`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AnalysisConfig {
    pub mode: AnalysisMode,
    pub exclude_tests: bool,
    pub exclude_traits: bool,
    pub exclude_ffi: bool,
    pub exclude_patterns: Vec<String>,
}
```

### struct `DeadCodeFilter`

```rust
#[derive(Debug)]
pub struct DeadCodeFilter<'a> {
    config: &'a AnalysisConfig,
    storage: Arc<dyn Storage>,
}
```

---

## `src/analyzer/mod.rs`

### struct `DeadCodeAnalyzer`

```rust
#[derive(Debug)]
pub struct DeadCodeAnalyzer {
    storage: Arc<dyn Storage>,
    config: AnalysisConfig,
}
```

---

## `src/analyzer/reachability.rs`

### struct `ReachabilityAnalyzer`

```rust
#[derive(Debug)]
pub struct ReachabilityAnalyzer {
    storage: Arc<dyn Storage>,
}
```

---

## `src/analyzer/report.rs`

### enum `ReportFormat`

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub enum ReportFormat {
    Text,
    Json,
    Markdown,
}
```

### struct `DeadCodeReport`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeadCodeReport {
    pub dead_symbols: Vec<SymbolId>,
    pub entry_points: Vec<Symbol>,
    pub summary: ReportSummary,
}
```

### struct `ReportSummary`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ReportSummary {
    pub total_symbols: usize,
    pub reachable: usize,
    pub unreachable: usize,
    pub by_kind: HashMap<String, usize>,
}
```

---

## `src/cache.rs`

### struct `CachedFileData`

Cached parsing + embedding results for a single file content hash.

```rust
#[derive(Debug, Serialize, Deserialize)]
pub struct CachedFileData {
    pub parsed: ParsedFile,
    pub embeddings: Vec<Vec<f32>>,
    pub embed_model: String,
    #[serde(default)]
    pub schema_version: u32,
}
```

### struct `GlobalCache`

```rust
#[derive(Debug)]
pub struct GlobalCache {
    base_dir: PathBuf,
}
```

---

## `src/clones.rs`

### struct `CloneGroup`

Symbols found to share a shape.

```rust
#[derive(Debug, Clone, serde::Serialize)]
pub struct CloneGroup {
    pub members: Vec<StructureRow>,
    pub distance: u32,
}
```

---

## `src/config.rs`

### struct `Config`

```rust
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
#[serde(default)]
pub struct Config {
    pub embedding: EmbeddingConfig,
    pub parser: ParserConfig,
    pub storage: StorageConfig,
    pub index: IndexConfig,
    pub ignore: IgnoreConfig,
}
```

### struct `ParserConfig`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(default)]
pub struct ParserConfig {
    pub max_file_size: u64,
    pub enabled_languages: Vec<String>,
}
```

### struct `StorageConfig`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(default)]
pub struct StorageConfig {
    pub backend: String,
}
```

### struct `IndexConfig`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(default)]
pub struct IndexConfig {
    pub hnsw_m: usize,
    pub hnsw_ef_construction: usize,
    pub hnsw_ef_search: usize,
}
```

### struct `IgnoreConfig`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(default)]
pub struct IgnoreConfig {
    pub patterns: Vec<String>,
    pub use_gitignore: bool,
    pub use_ignore_file: bool,
}
```

---

## `src/embed/candle.rs`

### struct `BertEmbeddings`

```rust
struct BertEmbeddings {
    word_embeddings: candle_nn::Embedding,
    token_type_embeddings: candle_nn::Embedding,
    layer_norm: LayerNorm,
}
```

### struct `BertSelfAttention`

```rust
struct BertSelfAttention {
    query: Linear,
    key: Linear,
    value: Linear,
    layer_norm_q: LayerNorm,
    layer_norm_k: LayerNorm,
    num_heads: usize,
    head_size: usize,
}
```

### struct `BertSelfOutput`

```rust
struct BertSelfOutput {
    dense: Linear,
    layer_norm: LayerNorm,
}
```

### struct `BertAttention`

```rust
struct BertAttention {
    self_attn: BertSelfAttention,
    output: BertSelfOutput,
}
```

### struct `BertGLUMLP`

```rust
struct BertGLUMLP {
    up_gated_layer: Linear,
    down_layer: Linear,
    intermediate_size: usize,
}
```

### struct `BertLayer`

```rust
struct BertLayer {
    attention: BertAttention,
    layer_norm_1: LayerNorm,
    layer_norm_2: LayerNorm,
    mlp: BertGLUMLP,
}
```

### struct `JinaBertModel`

```rust
struct JinaBertModel {
    embeddings: BertEmbeddings,
    layers: Vec<BertLayer>,
    alibi_bias: Tensor,
}
```

### struct `CandleEmbedder`

```rust
pub struct CandleEmbedder {
    model: Mutex<JinaBertModel>,
    tokenizer: Tokenizer,
    device: Device,
}
```

---

## `src/embed/config.rs`

### struct `EmbeddingConfig`

Configuration for embedding providers.

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(default)]
pub struct EmbeddingConfig {
    pub provider: String,
    pub model: String,
    pub dimensions: u32,
    pub batch_size: usize,
    pub api_key: Option<String>,
    pub api_base: Option<String>,
}
```

### struct `ConfigWrapper`

Wrapper for TOML config files that may contain embedding section.

```rust
#[derive(Debug, Deserialize)]
struct ConfigWrapper {
    #[serde(default)]
    embedding: EmbeddingConfig,
}
```

---

## `src/embed/error.rs`

### enum `EmbedError`

```rust
#[derive(Error, Debug)]
pub enum EmbedError {
    #[error("Embedding error: {0}")]
    Embedding(String),
    #[error("Config error: {0}")]
    Config(String),
    #[error("{0}")]
    Unavailable(String),
    #[error("IO error: {0}")]
    Io(std::io::Error),
    #[error("TOML parse error: {0}")]
    TomlParse(toml::de::Error),
    #[error("TOML serialize error: {0}")]
    TomlSerialize(toml::ser::Error),
}
```

### type `Result`

```rust
pub type Result<T> = std::result::Result<T, EmbedError>;
```

---

## `src/embed/mod.rs`

### struct `NoEmbedder`

The embedder a build without `candle` gets: it never embeds, and says so.

```rust
#[derive(Debug, Default)]
pub struct NoEmbedder;
```

---

## `src/error.rs`

### enum `Error`

```rust
#[derive(Error, Debug)]
pub enum Error {
    #[error("IO error: {0}")]
    Io(std::io::Error),
    #[error("SQLite error: {0}")]
    Sqlite(rusqlite::Error),
    #[error("JSON error: {0}")]
    Json(serde_json::Error),
    #[error("TOML parse error: {0}")]
    TomlParse(toml::de::Error),
    #[error("Configuration error: {0}")]
    Config(String),
    #[error("Parser error: {0}")]
    Parser(String),
    #[error("Embedding error: {0}")]
    Embedding(String),
    #[error("Index error: {0}")]
    Index(String),
    #[error("Storage error: {0}")]
    Storage(String),
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Invalid path: {0}")]
    InvalidPath(String),
    #[error("Unsupported language: {0}")]
    UnsupportedLanguage(String),
    #[error("File watcher error: {0}")]
    Watcher(notify::Error),
    #[error("Embed error: {0}")]
    Embed(crate::embed::EmbedError),
}
```

### type `Result`

```rust
pub type Result<T> = std::result::Result<T, Error>;
```

---

## `src/graph.rs`

### struct `SymbolMetrics`

Calculate metrics for a symbol in the call graph

```rust
#[derive(Debug, Clone)]
pub struct SymbolMetrics {
    pub symbol: Symbol,
    pub direct_callers: usize,
    pub direct_callees: usize,
    pub fan_in: usize,
    pub fan_out: usize,
    pub is_entry_point: bool,
    pub is_leaf: bool,
}
```

---

## `src/indexer/mod.rs`

### struct `FileProcessResult`

Result from processing a single file

```rust
struct FileProcessResult {
    symbols_count: usize,
    symbol_map: HashMap<String, SymbolId>,
    references: Vec<ParsedReference>,
}
```

---

## `src/lang/cpp.rs`

### enum `InternalSymbolKind`

Internal symbol kind (ABI-independent)

```rust
#[derive(Debug, Clone, Copy)]
pub enum InternalSymbolKind {
    Function,
    Method,
    Class,
    Struct,
    Enum,
    Namespace,
    Field,
}
```

### enum `InternalReferenceKind`

Internal reference kind (ABI-independent)

```rust
#[derive(Debug, Clone, Copy)]
pub enum InternalReferenceKind {
    Call,
    Import,
    TypeReference,
    FieldAccess,
    Inheritance,
}
```

### struct `InternalLocation`

Internal location (ABI-independent)

```rust
#[derive(Debug, Clone)]
pub struct InternalLocation {
    pub start_line: u32,
    pub start_col: u32,
    pub end_line: u32,
    pub end_col: u32,
    pub start_byte: u32,
    pub end_byte: u32,
}
```

### struct `InternalSymbol`

Internal symbol (ABI-independent)

```rust
#[derive(Debug, Clone)]
pub struct InternalSymbol {
    pub name: String,
    pub kind: InternalSymbolKind,
    pub location: InternalLocation,
    pub signature: Option<String>,
    pub children: Vec<InternalSymbol>,
}
```

### struct `InternalReference`

Internal reference (ABI-independent)

```rust
#[derive(Debug, Clone)]
pub struct InternalReference {
    pub name: String,
    pub kind: InternalReferenceKind,
    pub location: InternalLocation,
}
```

### struct `CppAnalyzer`

```rust
#[derive(Debug)]
pub struct CppAnalyzer;
```

---

## `src/lang/csharp.rs`

### struct `CSharpAnalyzer`

```rust
#[derive(Debug)]
pub struct CSharpAnalyzer;
```

---

## `src/lang/go.rs`

### struct `GoAnalyzer`

```rust
#[derive(Debug)]
pub struct GoAnalyzer;
```

---

## `src/lang/java.rs`

### struct `JavaAnalyzer`

```rust
#[derive(Debug)]
pub struct JavaAnalyzer;
```

---

## `src/lang/lua.rs`

### struct `LuaAnalyzer`

```rust
#[derive(Debug)]
pub struct LuaAnalyzer;
```

---

## `src/lang/php.rs`

### struct `PhpAnalyzer`

```rust
#[derive(Debug)]
pub struct PhpAnalyzer;
```

---

## `src/lang/python.rs`

### struct `PythonAnalyzer`

```rust
#[derive(Debug)]
pub struct PythonAnalyzer;
```

---

## `src/lang/ruby.rs`

### struct `RubyAnalyzer`

```rust
#[derive(Debug)]
pub struct RubyAnalyzer;
```

---

## `src/lang/rust.rs`

### struct `RustAnalyzer`

```rust
#[derive(Debug)]
pub struct RustAnalyzer;
```

---

## `src/lang/swift.rs`

### struct `SwiftAnalyzer`

```rust
#[derive(Debug)]
pub struct SwiftAnalyzer;
```

---

## `src/lang/typescript.rs`

### struct `TypeScriptAnalyzer`

```rust
#[derive(Debug)]
pub struct TypeScriptAnalyzer;
```

---

## `src/lib.rs`

### struct `Indexer`

An open index over one project directory.

```rust
#[derive(Debug)]
pub struct Indexer {
    project_path: PathBuf,
    config: Config,
    storage: Arc<dyn Storage>,
    embedder: Arc<dyn Embedder>,
    parser: Arc<dyn Parser>,
    index: Arc<dyn VectorIndex>,
    cache: Arc<GlobalCache>,
}
```

---

## `src/migrations/runner.rs`

### struct `SqlMigration`

One migration: a version, a name for the log, and the SQL that takes the schema forward.

```rust
#[derive(Debug, Clone)]
pub struct SqlMigration {
    pub version: i64,
    pub name: String,
    pub up_sql: String,
}
```

---

## `src/parser/treesitter/analyzers/base.rs`

### struct `AnalyzerBase`

Shared helper functions for all analyzers

```rust
#[derive(Debug)]
pub struct AnalyzerBase;
```

---

## `src/parser/treesitter/analyzers/generic.rs`

### struct `GenericAnalyzer`

```rust
#[derive(Debug)]
pub struct GenericAnalyzer {
    base: AnalyzerBase,
    language: Language,
}
```

---

## `src/parser/treesitter/mod.rs`

### struct `TreeSitterParser`

Tree-sitter parser over the grammars this build links in (see `crate::lang`).

```rust
#[derive(Debug)]
pub struct TreeSitterParser;
```

---

## `src/search/usearch.rs`

### struct `UsearchIndex`

```rust
pub struct UsearchIndex {
    index: Mutex<Index>,
    path: PathBuf,
    dimensions: usize,
}
```

---

## `src/storage/mmap.rs`

### struct `Header`

```rust
#[repr(C)]
#[derive(Clone, Copy, Pod, Zeroable)]
struct Header {
    magic: [u8; 4],
    version: u32,
    dimensions: u32,
    _padding: u32,
    count: u64,
    model_hash: [u8; 32],
}
```

### struct `EmbeddingStore`

```rust
#[derive(Debug)]
pub struct EmbeddingStore {
    path: std::path::PathBuf,
    dimensions: u32,
}
```

### struct `EmbeddingIterator`

```rust
#[derive(Debug)]
pub struct EmbeddingIterator {
    file: File,
    dimensions: usize,
    count: usize,
    current: usize,
}
```

---

## `src/storage/mod.rs`

### struct `StructureRow`

One symbol's structural fingerprint, with just enough alongside it to name the symbol in a report.

```rust
#[derive(Debug, Clone, serde::Serialize)]
pub struct StructureRow {
    pub id: SymbolId,
    pub name: String,
    pub kind: SymbolKind,
    pub file_path: PathBuf,
    pub start_line: u32,
    pub end_line: u32,
    pub structure: Structure,
}
```

---

## `src/storage/sqlite.rs`

### struct `SqliteStorage`

```rust
#[derive(Debug)]
pub struct SqliteStorage {
    conn: Mutex<Connection>,
}
```

---

## `src/structure.rs`

### struct `Structure`

The fingerprint of one symbol's syntax.

```rust
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct Structure {
    pub hash: String,
    pub simhash: u64,
    pub node_count: u32,
}
```

---

## `src/types.rs`

### struct `SymbolId`

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct SymbolId(pub i64);
```

### struct `FileId`

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct FileId(pub i64);
```

### enum `Visibility`

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub enum Visibility {
    Public,
    PublicCrate,
    PublicSuper,
    Protected,
    Private,
    Internal,
    Unknown,
}
```

### enum `SymbolKind`

Mirrors `lib_plugin_abi_v3::lang::SymbolKind`. New variants must be appended (never inserted) and use the matching discriminant from the ABI definition — see lang.rs in lib-plugin-abi-v3 for the ABI compatibility rules. The conversion `From<lang::SymbolKind>` in the parser plugin adapter relies on these staying in sync.

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[repr(u8)]
pub enum SymbolKind {
    Function = 0,
    Method = 1,
    Class = 2,
    Struct = 3,
    Enum = 4,
    Interface = 5,
    Trait = 6,
    Module = 7,
    Constant = 8,
    Variable = 9,
    Type = 10,
    Property = 11,
    Field = 12,
    Constructor = 13,
    Destructor = 14,
    Operator = 15,
    Macro = 16,
    Namespace = 17,
    Package = 18,
    Unknown = 19,
    Import = 20,
}
```

### struct `Location`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Location {
    pub start_line: u32,
    pub start_col: u32,
    pub end_line: u32,
    pub end_col: u32,
    pub start_byte: u32,
    pub end_byte: u32,
}
```

### struct `Symbol`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Symbol {
    pub id: SymbolId,
    pub name: String,
    pub kind: SymbolKind,
    pub file_id: FileId,
    pub file_path: PathBuf,
    pub location: Location,
    pub parent_id: Option<SymbolId>,
    pub signature: Option<String>,
    pub description: Option<String>,
    pub doc_comment: Option<String>,
    pub visibility: Visibility,
    pub is_entry_point: bool,
    pub structure: Option<Structure>,
}
```

### struct `File`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct File {
    pub id: FileId,
    pub path: PathBuf,
    pub language: Language,
    pub hash: String,
    pub size: u64,
    pub description: Option<String>,
}
```

### struct `FileInfo`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct FileInfo {
    pub file: File,
    pub symbols: Vec<Symbol>,
}
```

### struct `SearchResult`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SearchResult {
    pub symbol: Symbol,
    pub score: f32,
    pub context: Option<String>,
}
```

### struct `Tree`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Tree {
    pub files: Vec<FileNode>,
}
```

### struct `FileNode`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct FileNode {
    pub path: PathBuf,
    pub language: Language,
    pub symbols: Vec<SymbolNode>,
}
```

### struct `SymbolNode`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SymbolNode {
    pub id: SymbolId,
    pub name: String,
    pub kind: SymbolKind,
    pub children: Vec<SymbolNode>,
}
```

### struct `Status`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Status {
    pub indexed_files: u64,
    pub indexed_symbols: u64,
    pub embedding_dimensions: u32,
    pub embedding_model: String,
    pub last_indexed: Option<String>,
    pub storage_size_bytes: u64,
    pub pipeline_version: u32,
}
```

### struct `IndexProgress`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct IndexProgress {
    pub files_processed: u64,
    pub files_total: u64,
    pub symbols_indexed: u64,
    pub errors: Vec<String>,
}
```

### enum `Language`

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum Language {
    Rust,
    Python,
    JavaScript,
    TypeScript,
    Java,
    Go,
    C,
    Cpp,
    CSharp,
    Ruby,
    Php,
    Kotlin,
    Scala,
    Swift,
    Bash,
    Lua,
    Sql,
    Json,
    Yaml,
    Toml,
    Xml,
    Html,
    Css,
    Markdown,
    Dockerfile,
    Hcl,
    GraphQL,
    Haskell,
    OCaml,
    Elixir,
    Erlang,
    Zig,
    Unknown,
}
```

### enum `ReferenceKind`

Kind of reference between symbols

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub enum ReferenceKind {
    Call,
    TypeReference,
    FieldAccess,
    Import,
    Inheritance,
    MacroInvocation,
    VariableReference,
}
```

### struct `ParsedReference`

Unresolved reference found during parsing

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ParsedReference {
    pub name: String,
    pub kind: ReferenceKind,
    pub location: Location,
    pub containing_symbol_index: Option<usize>,
}
```

### struct `Reference`

Resolved reference stored in database

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Reference {
    pub from_symbol_id: SymbolId,
    pub to_symbol_id: SymbolId,
    pub kind: ReferenceKind,
    pub location: Location,
}
```

### struct `SymbolUsage`

Symbol usage statistics

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SymbolUsage {
    pub symbol: Symbol,
    pub reference_count: u64,
    pub callers: Vec<Symbol>,
    pub callees: Vec<Symbol>,
}
```

### struct `ParsedSymbol`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ParsedSymbol {
    pub name: String,
    pub kind: SymbolKind,
    pub location: Location,
    pub signature: Option<String>,
    pub doc_comment: Option<String>,
    pub children: Vec<ParsedSymbol>,
    pub visibility: Visibility,
    #[serde(default)]
    pub structure: Option<Structure>,
}
```

### struct `ParsedFile`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ParsedFile {
    pub language: Language,
    pub symbols: Vec<ParsedSymbol>,
    pub references: Vec<ParsedReference>,
}
```

---

## `src/watcher.rs`

### struct `Watcher`

```rust
#[derive(Debug)]
pub struct Watcher {
    project_path: PathBuf,
    config: Arc<Config>,
    tx: mpsc::UnboundedSender<Vec<PathBuf>>,
}
```


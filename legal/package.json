{
  "name": "adi-legal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generates the legal documents in this directory from the jurisdiction dataset.",
  "scripts": {
    "generate": "bun run generate.ts",
    "check": "bun run generate.ts --check",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.3"
  }
}
